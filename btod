#include <stdio.h>
int n;
int btod(long long n1);
int main() {
    long long n;
    printf("Enter a binary number: ");
    scanf("%lld", &n);
    printf("%lld in binary = %d in decimal", n, btod(n));
    return 0;
}
int btod(long long n1){
       
    return (n1%10*pow(2,i))+btod(n/10)
    
}
